#!/bin/zsh

set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "Usage: stanbook <path>" >&2
  echo "   or: stanbook --qa-diff <html-file>" >&2
  exit 2
fi

script_dir="${0:A:h}"

cd "$script_dir"
exec mvn -q exec:java -Dexec.args="$*"
